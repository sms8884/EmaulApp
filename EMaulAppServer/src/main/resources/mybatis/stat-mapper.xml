<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jaha.server.emaul.mapper.StatMapper">

	<insert id="insertSharer" parameterType="Map">
		insert into stat_sharer
		values (now(), #{postId}, #{userId}, #{sns})
	</insert>

</mapper>
